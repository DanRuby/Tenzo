<Window x:Class="TenzoActualGUI.View.old.PlayListRunner"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Проведение измерения" Height="300" Width="300">
    <DockPanel>
        <ListBox DockPanel.Dock="Bottom">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <TextBlock Text="слайд1"/>
            <TextBlock Text="слайд2"/>
        </ListBox>
        <TextBlock DockPanel.Dock="Bottom" Text="Выбран набор: набор 1 "/>
        <DockPanel>
            
        </DockPanel>
    </DockPanel>
</Window>
